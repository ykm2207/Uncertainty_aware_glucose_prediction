# 작업 로그 — 2026-08-03 (서버 학습 이후)

전날(8/2) 만든 CGMacros/Shanghai 파이프라인을 서버(CUDA)로 옮겨 실제로 Shanghai_T2DM
300epoch 본 학습을 처음 완주시킨 날. 서버 환경 세팅 과정에서 생긴 여러 문제들과,
그 이후 코드 정리·지표 재활성화 작업을 정리한다.

---

## 1. 폴더 구조 정리 (서버로 넘기기 전)

루트에 파이썬 파일이 21개까지 늘어나서 정리 필요성이 제기됨:
- HUPA 원본(`configs.py`/`data.py`/`model.py`/`train.py`/`evaluate.py`/`utils.py`)은 루트에 그대로 유지
- CGMacros 관련 파일 전부 `cgmacros/` 폴더로, Shanghai 관련 전부 `shanghai/` 폴더로 이동
- 파일명은 그대로 유지(`data_cgmacros.py` 등) — 폴더 안이라고 `data.py`로 줄이면 루트의
  공용 `data.py`와 이름이 겹쳐서 import가 꼬이는 문제가 있어서 일부러 안 줄임
- 하위 폴더에서도 루트 공용 모듈(`data.py`/`model.py`/`utils.py`)을 import할 수 있도록
  각 파일 상단에 `sys.path.insert(0, ...)`로 루트 경로를 추가
- 이동 후 세 파이프라인(CGMacros raw/revision, Shanghai) 전부 재검증 완료

## 2. 서버(CUDA) 환경 세팅 — 겪은 문제들과 해결

실제로 겪은 문제와 원인을 기록해둔다 (다음에 또 서버 세팅할 때 참고용):

1. **conda 없음**: 서버에 Anaconda/Miniconda가 아예 안 깔려있었음 → `python3 -m venv`로 대체
2. **torch 설치 실패("no matching distribution")**: 처음엔 원인 불명확했으나, 이후 사용자가
   직접 해결(정확한 원인 미확인 — 다음엔 `uname -m`으로 아키텍처부터 확인할 것)
3. **CUDA 버전 확인**: `nvidia-smi`로 드라이버가 지원하는 최대 CUDA 버전(13.2) 확인 →
   PyTorch 공식 페이지 기준 그 시점 최신 빌드였던 cu128 wheel로 설치
   (`pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128`)
4. **tmux 중첩 에러**("sessions should be nested with care"): 이미 tmux 세션 안에 있는
   상태에서 또 tmux를 켜려고 해서 발생. 그냥 기존 세션에서 이어서 실행하면 해결.
5. **ZIP 다운로드 vs git clone**: GitHub에서 "Download ZIP"으로 받아서 폴더명이
   `Uncertainty_aware_glucose_prediction-main`이 됐고, `.git`이 없어서 서버에서 바로
   push가 안 되는 상태였음.
6. **데이터 경로 문제**: Shanghai 데이터를 `data/` 폴더 없이 저장소 바로 아래에
   압축 해제해서(`~/.../ShangHai Datatset/...`), `configs_shanghai.py`가 기대하는
   `./data/ShangHai Datatset/...` 경로와 안 맞아 `세션 0개`로 조용히 실패함
   (에러를 안 내고 0개로 넘어가는 게 코드 허점 — 나중에 명시적 에러로 바꿀지 검토 필요).
   `find ~ -iname "*Shanghai*"`로 실제 위치 찾아서 `data/` 폴더 만들고 이동해서 해결.

## 3. Shanghai_T2DM 300epoch 학습 완료

- 서버에서 `python shanghai/train_shanghai.py` 실행, CUDA로 정상 완주
- Loss 곡선 확인(로컬로 pull해서 직접 확인): 초반 급락 후 매끄럽게 수렴,
  마지막 10epoch 상대변화율 중앙값 1.06e-3 → 사실상 수렴 완료, NaN 없음

## 4. 결과 push 과정에서 겪은 git 인증 문제들

1. **`jj8127`에게 권한 거부**: 이 서버에 `gh`(GitHub CLI)가 다른 계정(jj8127)으로
   이미 로그인돼 있어서, 모든 git 인증을 자동으로 그 계정으로 가로챔
   (`credential.https://github.com.helper=...gh auth git-credential`). 여러 명이
   같이 쓰는 서버로 추정.
2. **fine-grained 토�큰으로도 거부**: `github_pat_...` 형태의 fine-grained 토큰을
   발급받아 시도했으나, 저장소 접근 권한을 토큰 생성 시 명시적으로 안 줘서 여전히 거부됨.
   ⚠️ **이 과정에서 실제 토큰이 스크린샷에 그대로 노출되어 즉시 폐기 안내함.**
3. **해결**: **classic** 토큰(`repo` 스코프 체크)으로 재발급 → 문제 해결.
   fine-grained 토큰은 저장소별 권한을 세밀하게 관리해야 해서 이런 실수가 나기 쉬움 —
   개인 프로젝트에선 classic이 훨씬 간단.
4. `git add`만 하고 `git commit`을 빼먹어서 "Everything up-to-date"로 헷갈렸던 것,
   그다음엔 `git commit`에서 `user.name`/`user.email` 미설정으로 막힌 것도 순서대로 해결.
5. 최종적으로 `shanghai_tem_model.pth`, `shanghai_training_loss_plot.png` 두 파일만
   깔끔하게 커밋됨 (데이터 폴더가 실수로 안 딸려왔는지도 확인함 — `git diff --stat`로
   두 바이너리 파일만 변경됐음을 검증).

## 5. 원 논문 지표 재활성화

- CGMacros 결과(스모크 테스트)를 먼저 확인한 뒤, 아직 완전한 해석이 안 될까봐
  RMSE/MAE만 남기고 MARD/DTS/Brier/AUC/ECP는 주석처리해뒀던 상태였음
- 이번에 세 evaluate 스크립트(`evaluate_cgmacros.py`, `evaluate_cgmacros_revision.py`,
  `evaluate_shanghai.py`) 전부 주석 해제, RMSE/MAE와 함께 다시 계산되도록 복구
- 로컬에서 스모크 테스트로 정상 동작 확인 후 push

### ⚠️ 작업 중 실수 및 복구
지표 재활성화를 검증하는 스모크 테스트 도중, 서버에서 받아온 **진짜 300epoch 학습
결과 파일**(`shanghai_tem_model.pth`, `shanghai_training_loss_plot.png`)이 스모크
테스트용 임시 학습으로 덮어써지고 삭제되는 사고가 있었음. 다행히 이미 GitHub에
커밋되어 있어서 `git restore`로 즉시 복구, 데이터 손실 없음. (이후 스모크 테스트 시
결과물이 실제 서버 산출물과 파일명이 겹치지 않는지 더 주의할 것.)

## 6. 최종 결과 (Shanghai_T2DM, 300epoch, 전체 세션)

| 지표 | 값 | 해석 |
|---|---|---|
| RMSE | 9.844 | 30분 뒤 예측치고 오차 작은 편 |
| MAE | 6.327 | |
| MARD | 4.908% | 임상 통상 기준(10% 미만)의 절반 이하 — 우수 |
| Zone A+B 정확도 | 99.98% | 위험한 오진(C/D/E) 거의 없음 → 임상적으로 안전 |
| Level 70 Sensitivity (저혈당) | 0.952 | |
| Level 180 Sensitivity (고혈당) | 0.964 | |
| AUC_Hypo / AUC_Hyper | 0.915 / 0.966 | 저·고혈당 판별력 우수 |
| MCE (보정오차) | 0.011 | 매우 낮음 → 불확실성 보정이 잘 됨 |
| Correlation with error | 0.462 | 불확실성-실제오차 상관, 중간 정도 |
| Correlation with risk zones | 0.150 | 상대적으로 약한 지점 (유일한 약점) |

**종합**: T1D 대상이던 원 논문 설정을 T2DM 데이터에 적용했음에도, 점 예측 정확도(MARD)와
임상 위험구간 안전성(Zone A+B), 불확실성 보정(MCE) 모두 양호한 수준. 상황보고서에
긍정적으로 쓸 수 있는 결과.

## 7. 아직 남은 것
- CGMacros(raw/revision) 서버 300epoch 본 학습은 아직 실행 전
- 이동평균(MA) 정책은 여전히 사수 확인 대기 중 (Shanghai는 12스텝 확정, CGMacros는 미정)
- CGMacros 데이터(674MB)는 아직 서버로 옮기지 않음
