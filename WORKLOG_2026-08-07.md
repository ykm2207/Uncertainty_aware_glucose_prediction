# 작업 로그 — 2026-08-07

오늘 할 일: 이동평균(MA) 정책을 재설계 — 기존 고정값(CGMacros 200분 / Shanghai 67행=1005분)이
임의로 너무 길었다는 판단에 따라, 두 데이터셋 공통으로 30/60/180분 세 후보를 스윕하도록
코드를 바꾸고 서버에서 재학습할 준비를 마쳤다. horizon(30분/60분) 조건은 그대로 유지.

---

## 1. 실험 설계 변경

- **바뀐 것은 MA뿐**: horizon(30/60분), 데이터셋(CGMacros/Shanghai), 결측치 처리 방식 등
  나머지 조건은 8/4 완료분과 동일. MA 윈도우 후보만 30/60/180분으로 교체.
- **MA 미적용(0) 베이스라인은 재사용**: `cgmacros_revision_h30/h60`, `shanghai_h30/h60`
  (이미 300epoch 정식 학습 완료된 결과)를 그대로 쓰고, 이번엔 재학습하지 않는다.
- **신규 실험 매트릭스**: 2개 데이터셋 x 2개 horizon x 3개 MA 윈도우 = 12개 조합.

## 2. 코드 구조 변경 — "한 번의 학습 실행 안에서 MA를 순회"

기존에는 `run_remaining_experiments.sh`가 bash에서 `sed`로 config의
`APPLY_MOVING_AVERAGE`(True/False) 값 하나를 바꾼 뒤 `python train_*.py`를 조합마다
따로 실행하는 방식이었다. 이번엔 "이동평균을 바꿔가면서 진행하도록"이라는 요청에 맞춰
MA 스윕 자체를 파이썬 코드 안으로 옮겼다:

- `configs_cgmacros.py` / `configs_shanghai.py`: `APPLY_MOVING_AVERAGE`(bool) +
  `MA_WINDOW_MINUTES`(단일값) 조합을 **`MA_WINDOW_CANDIDATES_MINUTES = [30, 60, 180]`**
  리스트로 교체. 저장 경로도 고정 상수(`MODEL_SAVE_PATH` 등)에서
  **`model_save_path(ma_window_minutes)`** 같은 함수로 바꿔서, MA 값마다 다른 파일명
  (`_ma30`, `_ma60`, `_ma180`)을 자동 생성하도록 함.
- Shanghai는 15분 그리드라 `apply_causal_moving_average()`가 받는 window가 "분"이 아니라
  "행 개수"이므로, `ma_minutes_to_rows()` 함수로 30/60/180분을 2/4/12행으로 환산해서
  넘긴다(CGMacros는 1분 그리드라 분==행이라 별도 환산 불필요).
- `train_cgmacros_revision.py` / `train_shanghai.py` (그리고 미사용 raw 파이프라인인
  `train_cgmacros.py`까지 동일 구조로): `main()`이 `MA_WINDOW_CANDIDATES_MINUTES`를
  순회하며 각 MA 값마다 데이터 준비 → 300epoch 학습 → 저장을 반복하는 구조로 변경.
  `evaluate_*.py`도 동일하게 후보별로 저장된 모델을 순회하며 평가하도록 변경.
- **horizon(30/60분)은 여전히 외부에서 제어**: `run_ma_sweep.sh`(구
  `run_remaining_experiments.sh`를 이름 변경 + 재작성)가 `HORIZON_MINUTES`만 sed로
  바꿔가며 train/evaluate 스크립트를 horizon당 한 번씩 실행 — 그 안에서 MA 3개 후보는
  파이썬이 알아서 전부 돈다. horizon마다 학습+평가+커밋+푸시까지 자동 처리.

## 3. 아직 서버에서 실행 전

이번 세션에서는 코드 변경 + 로컬 문법/설정 검증(`py_compile`, config import 테스트)까지만
했고, 실제 300epoch x 12개 조합 재학습은 서버(CUDA)에서 `cgmacros/run_ma_sweep.sh`,
`shanghai/run_ma_sweep.sh`를 실행해야 진행된다 (시간이 오래 걸려서 자동으로 실행하지 않음).
