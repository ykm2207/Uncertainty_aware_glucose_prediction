# 작업 로그 — 2026-08-02

## 배경 / 목표

기존 저장소는 HUPA-UCM 데이터셋(5분 간격 CGM)에 맞춰진 evidential Transformer
(TEM, `model.py`의 `e_Transformers`, 원 논문: Tan & McBeth, arXiv:2603.04955) 학습/평가
파이프라인이었다. 이를 우리 데이터셋(CGMacros, Shanghai_T2DM)과 실험 조건에 맞게
재구현하는 것이 오늘의 목표.

**주어진 실험 조건**
- CGMacros: 인슐린 제외, 36타임스텝, 이동평균 200 (사수 답변 오면 변경 가능)
- Shanghai: T2DM만 사용, 인슐린 제외, 12타임스텝, 이동평균 67
- 모델: TEM (기존 `e_Transformers` 그대로)
- 학습: Epoch 300, batch 1024, lr 1e-4, horizon 30분/60분, mps/cuda 둘 다 지원(지금은 mps로 확인)
- 환자 수/epoch/batch는 맥북 한계에 따라 가변
- 지표: RMSE/MAE (원 논문 지표 사용 시와 달라지는 점 보고)
- 모든 주석은 한글로, 왜 이렇게 구현했는지 자세히

---

## 1. 사전 조사 — 원본 코드베이스 파악

`configs.py`/`data.py`/`model.py`/`train.py`/`evaluate.py`/`utils.py`를 전부 읽고 구조 파악:
- `e_Transformers`: Transformer 인코더 + evidential 출력(alpha, beta, nu, gamma)
- `evidential_data_loss` + KL/Amini 정규화 손실 (원 논문 저자 코드)
- `data.py`의 `make_windows`/`split_data`/`extract_features`/`compute_means_variances`/
  `normalize_features`/`BGDataset`은 데이터셋에 종속되지 않는 순수 함수임을 확인
  → 이후 재사용하기로 결정
- `evaluate.py`는 MARD, DTS 존 정확도(A~E), Brier/AUC(저·고혈당), ECP/MCE, Spearman 상관 등
  "임상 위험구간 탐지 + 불확실성 보정" 위주의 지표를 씀 (RMSE/MAE는 없었음)

## 2. 데이터셋 실측 조사

로컬에 이미 존재하던 원본 데이터(`/Users/yoo/test_dataset/CGMacros`,
`/Users/yoo/Downloads/ShangHai Datatset`)를 직접 열어서 확인:

**CGMacros**
- 1분 간격 그리드. `Libre GL`은 실제로는 15분 간격 실측을 1분 단위로 선형보간한 값
  (2차 차분 ≈ 0 비율 약 94%)
- `Dexcom GL`은 사용 안 함(사용자 지정)
- HR/Calories(Activity) 결측 평균 각각 10.9%/6.2%, 최대 연속 결측 구간 중앙값 862분(~14시간)
- **CGMacros-007은 `Calories (Activity)` 컬럼 자체가 없음**(대신 `Steps`) — 코드로 자동 처리 필요

**Shanghai_T2DM**
- 15분 간격, 109개 세션 파일(환자당 방문 1~2회)
- 컬럼 표기가 3가지 변형(식이 컬럼명 '饮食'/'进食量' 등) 존재하나 `Date`, `CGM (mg / dl)`는 공통
- 109개 중 **89개가 구버전 `.xls`, 20개가 `.xlsx`** → `.xls`를 읽으려면 `xlrd` 패키지가
  꼭 필요함을 확인 (없으면 82%의 데이터를 못 읽음)

## 3. 이동평균(MA) 실측 검증 — 가장 중요한 발견

사용자가 확인해 준 정보(원 논문은 5분×36스텝=3시간 입력이 원칙, 현재 CGMacros
설정은 36분으로 1/5 수준)를 바탕으로, MA200(CGMacros)/MA67(Shanghai) 을 실제 원본
신호에 적용했을 때 저혈당(<70)/고혈당(>180) 이벤트가 어떻게 되는지 5명씩 직접 계산:

| 데이터셋 | 환자/세션 | 원본 저혈당 | MA 적용 후 |
|---|---|---|---|
| CGMacros-001 | Libre GL | 128건(0.87%) | **0건** |
| CGMacros-004 | Libre GL | 652건(4.64%) | **0건** |
| Shanghai 2000 | CGM | 저 62/고 87건 | **저 0 / 고 0건** |
| Shanghai 2001_0 | CGM | 저 4/고 106건 | **저 0 / 고 0건** |

→ MA를 타깃(Y)에도 적용하면 논문의 핵심 평가축(저혈당 민감도, DTS 존, Brier/AUC)이
무의미해지고 RMSE도 인위적으로 낮아짐을 확인. 중심(center=True) 방향 MA는 미래 시점
정보가 섞이는 누출 위험도 있음(일부 환자에서 causal 방향과 결과가 달라짐을 확인).

**결정**: 사수 답변이 올 때까지 이번 구현 범위에서는 **MA 미적용**, 원시 windowing만으로
파이프라인을 먼저 구축하기로 함.

## 4. 설계 결정 (사용자와 확인한 사항)

- 저장소 구조: 데이터셋별 **완전 분리 스크립트** (`*_cgmacros.py`, `*_shanghai.py`),
  기존 HUPA 파이프라인은 그대로 유지
- 원본 데이터: 레포 내부 `./data/CGMacros`, `./data/ShangHai Datatset/Shanghai_T2DM`로
  복사해서 사용 (688MB → `.gitignore`에 `/data/` 추가)
- Feature 구성: 데이터셋별 단일 `FEATURES` 리스트만 사용 (조합 실험용 리스트 구조는 안 씀)
- `make_windows`/`split_data`/`extract_features`/`compute_means_variances`/
  `normalize_features`/`BGDataset`은 `data.py`에서 그대로 import해서 재사용
  (완전분리 원칙은 config/train/evaluate 드라이버 단위로만 적용)

## 5. 구현한 파일

| 파일 | 역할 |
|---|---|
| `.gitignore` | `/data/`(원본 데이터) 제외 |
| `requirements.txt` | `openpyxl`, `xlrd>=2.0.1` 추가 (Shanghai `.xls` 파싱용) |
| `utils.py` (기존 파일에 추가) | `rmse()`, `mae()`, `get_device()`(mps→cuda→cpu 자동 폴백) |
| `configs_cgmacros.py` | CGMacros 실험 설정 (1분 간격, 36스텝, horizon 분→스텝 자동 환산, `MAX_GAP_FOR_INTERP_MIN=15` 등) |
| `data_cgmacros.py` | 환자 자동 탐색, 컬럼 유연 매칭(007번 칼로리 결측 컬럼 자동 처리), **결측 구간 길이에 따라 보간/절단을 나누는 `segment_by_gaps()`**, 결측 진단 리포트 `report_missingness()` |
| `train_cgmacros.py` / `evaluate_cgmacros.py` | 학습/평가 (RMSE·MAE를 1차 지표로, 원 논문 지표는 참고용으로 함께 출력 + 차이점 설명 문구 출력) |
| `configs_shanghai.py` | Shanghai 실험 설정 (15분 간격, 12스텝, T2DM 폴더만 참조) |
| `data_shanghai.py` | 세션(방문) 단위 탐색/로딩(`.xls`/`.xlsx` 모두 지원, 컬럼명 변형 흡수), 세션 길이 진단 `report_session_lengths()` |
| `train_shanghai.py` / `evaluate_shanghai.py` | 학습/평가 (T2DM 사용에 따른 주의사항 안내 문구 포함) |

## 6. 검증 (✅ 실제 MPS 실행으로 확인)

- `report_missingness()` 실행 결과: CGMacros 환자 평균 결측 후 사용 가능 비율 **87.0%**,
  `CGMacros-007`은 사용 가능 비율 0%로 자동 제외됨을 확인
- `report_session_lengths()`: Shanghai 109개 세션 전부 읽기 성공, 전부 사용 가능 길이
- CGMacros: 환자 2명, epoch 2로 학습 → 평가까지 MPS에서 정상 동작
  (예: RMSE 13.4, MAE 7.4, 테스트셋에 고혈당 샘플 없어 Hyper 지표 자동으로 "정의 불가" 처리됨을 확인)
- Shanghai: 세션 10개, epoch 2로 학습 → 평가까지 MPS에서 정상 동작
  (예: RMSE 15.4, MAE 11.1)
- 전체 신규 파일 `py_compile` 문법 검사 통과

## 7. 미해결/확인 필요 사항 (다음에 이어서 볼 것)

- **이동평균 정책**: X에만 적용할지, causal 방향으로 할지, 윈도우 200/67의 정확한 의도가
  무엇인지 사수 답변 대기 중. config에 `MAX_GAP_FOR_INTERP_MIN` 등은 준비돼 있으나 MA
  자체는 아직 코드에 없음.
- **원본 train.py의 학습/검증 데이터 분리 방식**을 그대로 복제함: `train_val_loader`
  (train+val 합본)로 학습하고 `val_loader`로 모니터링 — val이 학습 데이터에도 포함돼
  있어 완전한 held-out 검증은 아님. 원 논문 저자 코드의 기존 관례라 손대지 않았지만
  결과 해석 시 감안 필요.
- **T1D 논문 모델을 T2DM 데이터에 적용**하는 것이 타당한지는 실제 300epoch 학습 후
  `evaluate_shanghai.py`의 MCE/Correlation with error 등을 보고 판단해야 함.
- 본 학습(300 epoch)은 아직 실행 안 함 — `PATIENT_LIMIT`/`SESSION_LIMIT`을 `None`으로
  둔 채 `python train_cgmacros.py && python evaluate_cgmacros.py` (Shanghai도 동일 패턴)로
  직접 실행 필요.

## 8. 커밋 여부

오늘 작업한 코드는 아직 **커밋하지 않음** (git status: 신규/수정 파일 다수, 스테이징 안 됨).
