# 작업 로그 — 2026-08-04

오늘 할 일: (1) CGMacros 스텝수/이동평균이 논문과 맞게 반영됐는지 확인, (2) CUDA로 학습,
(3) 결과보고. 실제로는 이동평균 정책을 사수 답변 없이 "일단 지정값대로 해보고 안 좋으면
조정"하는 쪽으로 진행하게 됐고, 그 과정에서 horizon(30/60분) 조건을 빠뜨렸던 것도 발견해서
최종적으로 CGMacros/Shanghai 각각 8개 조합(MA 적용×2 × horizon×2 = 4개씩)을 전부
300epoch 정식 학습까지 완료했다.

---

## 1. CGMacros 5분 리샘플링 구현

- 확인 결과 `data_revision`이 여전히 1분 그리드 그대로였음 (리샘플링 미반영)
- `data_cgmacros.decimate_segment()` 추가: 결측 절단(1분 그리드 기준) 이후 단계에서
  5행마다 1개씩 뽑아 5분 간격으로 다운샘플링
- `INPUT_TIMESTEPS=36`은 그대로 두되 `SAMPLE_INTERVAL_MIN`을 1→5로 바꿔서
  36×5분=180분(3시간) 입력창이 원 논문(HUPA, 5분×36스텝)과 정확히 일치하게 됨
- ⚠️ 한계: Libre GL 실측 주기가 15분이라 5분 그리드도 값의 2/3는 여전히 보간값
  (1분 그리드보다는 실측 비율이 6.7%→33%로 개선되긴 함)

## 2. 이동평균(MA) 구현 — CGMacros(200), Shanghai(67)

- `apply_causal_moving_average()`를 공용 `utils.py`로 옮겨서 CGMacros/Shanghai가 같이 사용
  (data.py는 원 논문 HUPA 파일이라 손대지 않는 원칙 유지)
- 핵심 설계: **입력(X)에만 과거방향(causal) 이동평균 적용, 타깃(Y)은 항상 원본 유지**
  (8/2 실측 검증: 타깃까지 스무딩하면 저혈당 이벤트가 사라져서 임상 지표가 무의미해짐)
- `make_windows(X, Y, L, H)`가 X/Y를 독립된 배열로 받는 구조를 활용해서,
  raw/smoothed 두 배열을 각각 만들고 X는 smoothed, Y는 raw에서 추출하는 방식으로 구현
- CGMacros: MA_WINDOW=200 (1분 그리드 기준, 다운샘플링 전 적용)
- Shanghai: MA_WINDOW=67 (15분 그리드 기준 = 1005분≈16.75시간)

## 3. 서버(CUDA) 트러블슈팅 (오늘 겪은 것들)

- `CGMacros_revision`(구글드라이브로 미리 준비된 결측 절단본)을 발견 → 8/3 버그 수정
  이후 버전인지 CGMacros-004의 Libre GL NaN 개수(0개, 14236행)로 직접 검증 후 재사용
  (원본 674MB 재전송 안 해도 됨 → 인터넷 느려서 시간 절약)
- 여러 번 반복된 경로 문제: `shanghai/` 폴더 안에서 실행하면 상대경로(`./data/...`)가
  깨져서 "train 데이터 없음" 에러 → 항상 레포 루트에서 실행해야 함을 재확인
- tmux 중첩 세션 에러, 새 터미널 창으로 여러 세션 동시 실행하는 법 안내

## 4. "성능이 목표가 아니다" — 중요한 관점 정리

MA 적용 시 CGMacros/Shanghai 둘 다 RMSE/MAE/MARD/Zone정확도/민감도가 뚜렷하게
나빠지는 걸 확인하고, 사용자가 "더 나은 예측성능이 우리 목표냐"고 되물음.
→ 이 프로젝트의 목적은 **원 논문 방법론(지정된 MA 조건 포함)을 재현하고 결과를
보고하는 것**이지 하이퍼파라미터 튜닝이 아님. 그래서 "MA가 나쁘니 버리자"가 아니라
**MA 적용/미적용 두 버전을 다 300epoch 정식으로 만들어서 나란히 비교 보고**하는
방향으로 결정.

## 5. horizon(30분/60분) 조건 누락 발견 및 보완

- 원래 조건에 "horizon 30분/60분 둘 다"가 있었는데 8/4까지 30분만 진행했던 것을 발견
- 저장 파일명에 `_ma`(MA 적용 여부) + `_h30`/`_h60`(horizon) 접미사를 자동으로 붙이도록
  `configs_cgmacros.py`/`configs_shanghai.py` 수정, 기존 완료 파일들도 이름 정리
- `cgmacros/run_remaining_experiments.sh`, `shanghai/run_remaining_experiments.sh`:
  남은 조합(CGMacros 3개, Shanghai 2개)을 config 자동 수정 + 학습 + 평가 + 커밋+ 푸시까지
  순서대로 알아서 처리하는 스크립트 작성 (오늘 안에 8개 조합 다 끝내야 해서 자동화)
- 서버에서 두 스크립트를 tmux 세션 2개로 동시 실행 → 전부 정상 완료, 각 단계마다 자동 push됨

## 6. 최종 결과 — 8개 조합 전체 비교

### CGMacros (결측 절단본, TEM)

| MA | Horizon | RMSE | MAE | MARD | Zone A | 저혈당 민감도 | 고혈당 민감도 | MCE |
|---|---|---|---|---|---|---|---|---|
| 미적용 | 30분 | **7.20** | **4.01** | **3.58%** | 98.4% | 0.981 | 0.970 | 0.039 |
| 미적용 | 60분 | 14.02 | 7.83 | 6.82% | 92.6% | 0.936 | 0.886 | 0.020 |
| 적용(200) | 30분 | 16.57 | 11.04 | 9.78% | 88.0% | 0.881 | 0.844 | 0.004 |
| 적용(200) | 60분 | 20.44 | 13.23 | 11.58% | 83.5% | 0.841 | 0.777 | 0.013 |

### Shanghai_T2DM (TEM)

| MA | Horizon | RMSE | MAE | MARD | Zone A | 저혈당 민감도 | 고혈당 민감도 | MCE |
|---|---|---|---|---|---|---|---|---|
| 미적용 | 30분 | **9.85** | **6.33** | **4.91%** | 97.4% | 0.952 | 0.964 | 0.011 |
| 미적용 | 60분 | 16.22 | 10.16 | 7.79% | 91.5% | 0.905 | 0.914 | 0.006 |
| 적용(67) | 30분 | 25.44 | 18.70 | 14.95% | 74.7% | 0.674 | 0.846 | 0.011 |
| 적용(67) | 60분 | 28.29 | 20.68 | 16.48% | 70.5% | 0.642 | 0.786 | 0.012 |

### 확인된 일관된 패턴 2가지
1. **이동평균 적용 시 두 데이터셋·두 horizon 전부에서 성능이 뚜렷하게 나빠짐.**
   추정 원인: MA가 입력의 "최근 변동 추세" 정보를 지워버려서 모델이 예측에 쓸 단서를
   잃음. 특히 Shanghai는 MA 윈도우(16.75시간)가 입력창(3시간)보다 5배 이상 길어서
   더 심함 (CGMacros는 MA 윈도우가 입력창과 비슷한 크기라 상대적으로 덜 심함).
2. **60분 예측이 30분보다 항상 더 어려움** (모든 지표 악화) — 예측 범위가 멀수록
   불확실성이 커지는 당연한 결과라, 파이프라인이 정상 작동함을 보여주는 방증이기도 함.

### 최고/최저 성능
- 최고: CGMacros, MA 미적용, 30분 (MARD 3.58%, Zone A 98.4%)
- 최저: Shanghai, MA 적용, 60분 (MARD 16.48%, Zone A 70.5%)

## 7. 아직 남은 것 / 의도적으로 제외한 것
- CGMacros **raw**(원본 데이터 직접 사용) 파이프라인은 원본 674MB를 서버로 못 옮겨서
  이번엔 제외 — 대신 동일한 결측치 처리 로직을 쓰는 revision 파이프라인으로 대체
- 이동평균 정책(200/67이 맞는지, 방향/윈도우 크기 등)은 여전히 사수 최종 확인 필요 —
  일단 지정값대로 돌려서 결과를 만들어뒀으니, 이 결과를 보여드리고 다음 방향 논의 예정
